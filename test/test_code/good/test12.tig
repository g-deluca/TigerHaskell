/* valid for and let */

let
	var a := 0
	function g (a: int) : int = (print("got out of loop"); a)
in 
	for a :=1 to 10 do (
		print("inside loop\n");
		if(a = 10) then print("llegamos a 10\n")
	);
	g(a)
end
