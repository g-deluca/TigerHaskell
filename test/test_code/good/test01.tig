/* an array type and an array variable */
let
  type  arrtype = array of int
  var arr1 : arrtype := arrtype [10] of 3
  function g(a: int) : int = arr1[0] + a
in
  arr1; if (arr1[0] = 0) then print("Es cero") else print("No es cero"); g(42)
end
